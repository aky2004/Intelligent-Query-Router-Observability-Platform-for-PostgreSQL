import { createFileRoute } from "@tanstack/react-router";
import { Fingerprint, KeyRound, RefreshCw, ShieldCheck } from "lucide-react";
import { PublicHeader, SiteFooter, StatusBadge } from "@/components/PublicShell";
export const Route=createFileRoute("/security")({head:()=>({meta:[{title:"Security — pg-router-ai"},{name:"description",content:"Learn how pg-router-ai protects sessions, credentials and database access."},{property:"og:title",content:"Security — pg-router-ai"},{property:"og:description",content:"Short-lived access, rotated refresh sessions and verified account ownership."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Security});
function Security(){const items=[[KeyRound,"SHORT-LIVED ACCESS","Signed access tokens expire quickly and are verified on every protected API and socket connection."],[RefreshCw,"ROTATION BY DEFAULT","Refresh tokens rotate on every use. Reuse revokes the entire token family."],[Fingerprint,"OTP OWNERSHIP","One-time codes are hashed, attempt-limited, single-use and expire after ten minutes."],[ShieldCheck,"ROLE ISOLATION","Roles remain separate from profile fields and every privileged action is checked server-side."]] as const;return <div className="mosaic min-h-screen"><PublicHeader/><main className="pt-16"><section className="border-b px-4 py-20 md:px-8"><div className="mx-auto max-w-[1200px]"><StatusBadge children="SECURITY POSTURE / VERIFIED"/><h1 className="mt-8 max-w-4xl font-display text-6xl font-bold leading-[.9] text-primary md:text-8xl">Trust is structural.</h1><p className="mt-8 max-w-xl text-muted-foreground">Every session, account action and real-time connection is verified at the server boundary.</p></div></section><section className="mx-auto grid max-w-[1200px] gap-px border-x bg-grid md:grid-cols-2">{items.map(([Icon,t,b])=><article key={t} className="bg-background p-8"><Icon className="size-7 text-primary"/><h2 className="mt-16 tech-label">{t}</h2><p className="mt-4 max-w-lg font-display text-2xl font-bold leading-tight">{b}</p></article>)}</section></main><SiteFooter/></div>}
